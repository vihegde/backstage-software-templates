# Docuemnts for ${{values.app_name}}
This  application has 2 endpoints
- `/api/v1/info`
- `/api/v1/healthz`

Here you could expand on what each of these endpoints do.

# How to access the app
You cn access the app by accessing the URL `${{values.app_name}}-${{values.app_env}}.test.com/api/v1/healthz` and '${{values.app_name}}-${{values.app_env}}.test.com/api/v1/details'
